spring:
  jpa:
    properties:
      hibernate:
      show_sql: true
      format_sql: true
      use_sql_comments: true
      type: trace
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql: trace